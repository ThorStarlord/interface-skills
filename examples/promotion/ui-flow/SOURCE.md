# SOURCE.md

No external fixtures could be copied or processed because the test harness script (`scripts/run-promotion-suite.py`) is missing.
When the test harness is implemented, this directory will contain the minimal required references from shared fixtures.
