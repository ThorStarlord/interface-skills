This directory is intentionally empty for now.

No isolated fixtures were copied into `examples/promotion/ui-spec-reconcile/`
because the promotion harness is not yet available in this repo snapshot.
Fixture population for this directory should be performed by
`scripts/run-promotion-suite.py` once that harness is present.
